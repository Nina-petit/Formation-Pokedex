<%- include('partials/header') %>

        <h1>Cliquez sur un type pour voir tous les Pokémon de ce type</h1>

        <div class="typesContainer">
            <% types.forEach((type) => { %>
                <a href="/types/<%=type.id%>" class="typeTag" style="background-color: #<%=type.color%>">
                    <%=type.name%>
                </a>
            <% }) %>
        </div>

    </main>
</body>
</html>